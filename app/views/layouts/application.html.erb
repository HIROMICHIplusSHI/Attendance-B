<!DOCTYPE html>
<html>
  <head>
    <title><%= yield(:title) + " | " if content_for?(:title) %>勤怠管理アプリ</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  </head>

  <body>
    <div class="container">
      <% if logged_in? %>
        <div class="text-right">
          <%= link_to "ログアウト", logout_path, data: { "turbo-method": :delete } %>
        </div>
      <% end %>
      <% flash.each do |message_type, message| %>
        <div class="alert alert-<%= message_type == 'success' ? 'info' : message_type %>">
          <%= message %>
        </div>
      <% end %>
      <%= yield %>
    </div>
  </body>
</html>
